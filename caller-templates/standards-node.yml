# Drop this file into a standalone-node consuming repo at:
#   .github/workflows/standards.yml
#
# Pin the workflow ref to a release tag (@v1.x.x), never @main in production.

name: Starisian Standards

on:
  pull_request:
  push:
    branches: [main, develop]

jobs:
  pnpm:
    uses: Starisian-Technologies/starisian-coding-standards/.github/workflows/pnpm-enforcement.yml@main
    with:
      mode: required
      profile_version: v1

  node:
    needs: pnpm
    uses: Starisian-Technologies/starisian-coding-standards/.github/workflows/node-enforcement.yml@main
    with:
      repo_type: standalone-node
      profile_version: v1
      enforcement_mode: required
      node-version: '22'

  media:
    uses: Starisian-Technologies/starisian-coding-standards/.github/workflows/media-enforcement.yml@main
    with:
      enforcement_mode: required
      profile_version: v1
