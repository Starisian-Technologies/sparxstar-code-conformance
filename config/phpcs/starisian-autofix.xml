<?xml version="1.0"?>
<!--
    Starisian Technologies — safe autofix ruleset (phpcbf).
    Ref: docs/php-wordpress-standard.md §8 (formatting).

    This ruleset contains ONLY rules that phpcbf can auto-fix safely without
    changing behaviour: whitespace, indentation, brace style, blank lines.
    It deliberately excludes rules that require human judgment (naming, security,
    i18n, database).

    Usage (run from consuming repo root), using PHP_CodeSniffer's "standard" option
    set to this file's path:
        vendor/bin/phpcbf "standard"=vendor/starisian-technologies/coding-standards/config/phpcs/starisian-autofix.xml src/
    (NOTE: a literal double-hyphen is invalid inside an XML comment per the XML
    spec — PHPCS's ruleset loader rejects it outright. Prefix the option name
    above with two hyphens yourself when you actually run the command.)

    After autofixing, run starisian-strict.xml to catch any remaining violations.
-->
<ruleset xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         name="StarisianAutofix"
         xsi:noNamespaceSchemaLocation="https://raw.githubusercontent.com/squizlabs/PHP_CodeSniffer/master/phpcs.xsd">

    <description>Starisian Technologies — safe autofix ruleset for phpcbf. Formatting only.</description>

    <arg value="sp"/>
    <arg name="extensions" value="php"/>
    <arg name="parallel" value="8"/>

    <config name="php_version" value="80200"/>

    <!-- ── Exclusions ── -->
    <exclude-pattern>*/vendor/*</exclude-pattern>
    <exclude-pattern>*/node_modules/*</exclude-pattern>
    <exclude-pattern>*/build/*</exclude-pattern>
    <exclude-pattern>*/dist/*</exclude-pattern>

    <!-- ── PSR-12 formatting rules (all fixable) ── -->
    <rule ref="PSR12.Files.FileHeader"/>
    <rule ref="PSR12.Files.OpenTag"/>
    <rule ref="Generic.Files.LineEndings">
        <properties>
            <property name="eolChar" value="\n"/>
        </properties>
    </rule>
    <rule ref="Generic.Files.ByteOrderMark"/>
    <rule ref="Generic.Formatting.SpaceAfterCast"/>
    <rule ref="Generic.Formatting.SpaceAfterNot"/>
    <rule ref="Generic.PHP.LowerCaseConstant"/>
    <rule ref="Generic.PHP.LowerCaseKeyword"/>
    <!--
        tabIndent=true: starisian-strict.xml disables DisallowTabIndent and pulls in
        WordPress-Core, which requires tab indentation. Autofix must converge to the
        same result strict accepts — space indentation here would make phpcbf output
        that phpcs then rejects on the very next run.
    -->
    <rule ref="Generic.WhiteSpace.ScopeIndent">
        <properties>
            <property name="indent" value="4"/>
            <property name="tabIndent" value="true"/>
        </properties>
    </rule>
    <rule ref="PSR2.ControlStructures.SwitchDeclaration"/>
    <rule ref="PSR2.Methods.MethodDeclaration"/>
    <rule ref="PSR2.Classes.PropertyDeclaration"/>
    <rule ref="Squiz.Functions.FunctionDeclarationArgumentSpacing">
        <properties>
            <property name="equalsSpacing" value="1"/>
        </properties>
    </rule>
    <rule ref="Squiz.WhiteSpace.SuperfluousWhitespace"/>
    <rule ref="Squiz.WhiteSpace.OperatorSpacing"/>

    <!-- ── WordPress-style spacing (fixable) ── -->
    <rule ref="WordPress.Arrays.ArrayIndentation"/>
    <rule ref="WordPress.WhiteSpace.ControlStructureSpacing"/>
    <rule ref="WordPress.WhiteSpace.OperatorSpacing"/>

</ruleset>
