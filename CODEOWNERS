# Coding standards (organization-wide)
# All changes require owner approval — this repo is the HOW for every product,
# every client, forever. The WHY / WHAT (ADRs, invariants, OQs) lives in each
# product's own decision registry, never here.
* @starisian-technologies/standards-owners
